const path = require("path");
const core = <reqSync_file>require("./core/shared/reqSync/RS_server.js");
const {helpers} = core;

core.analyse("./compiled",function(){
    //const base = helpers.nodeJS.readFile("compiled/core/shared/reqSync/RS_client.js");
    //helpers.parseJS?.completedFile("MVC");
});